//배열 고차함수 
const arr3= [1,2,3,4,5];
// const nums = [1,10,9,4,5];
const forEachArr = arr3.forEach(ele =>  console.log(ele));
console.log(forEachArr);

//map: 배열의 각 요소마다 콜백 적용, 적용 결과가 반영된 배열 반환
const mapArr = arr3.map(ele =>ele*ele);
//배열을 하나씩 훑으면서, 각 요소를 “변신시킨 값들”로 새로운 배열을 만들어주는 친구야!

console.log(mapArr);//[ 1, 4, 9, 16, 25 ]

//filter: 배열의 각 요소마다 콜백 적용, 적용 결과가 true인 요소들을 추출한 배열 반환
const filterArr = arr3.filter(ele=>ele%2);// 홀수만 필터링
console.log(filterArr); //[ 1, 3, 5 ]

//reduce: 배열의 요소마다 콜백을 적용, 
// 적용한 결과를 다음 콜백 호출시 전달해서 사용 
const sum = arr3.reduce((acc,curr)=> acc+curr,0); // 인자: 콜백, acc의 초기값 
//reduce 배열의 모든 요소를 하나로 “모으는” 친구야.
/**acc (accumulator) — 모아둔 값, 누적값

지금까지 계산된 중간 결과

✔ curr — 현재 처리 중인 배열 요소
쿠키 4개를 합쳐서 “총 몇 칼로리인지” 계산한다고 해보자.

curr = 지금 보는 쿠키

acc = 지금까지 더해온 총 칼로리

쿠키 하나씩 보면서 계속 더하는 작업 → reduce
 */
console.log(sum); //15

//some:콜백함수의 결과가 true인 경우가 하나라도 있으면 true를 반환 
const some = arr3.some(ele => ele==5);
console.log(some); //true

//every: 콜백함수의 모든 결과가 true인 경우, true를 반환 
const every = arr3.every(ele=>ele<10);
console.log(every);//true

//find: 콜백함수의 결과가 true인 첫번째 요소를 반환 
const find = arr3.find(ele => ele>3);
console.log(find);

//findIndex: 콜백함수의 결과가  true인 첫번째 요소의 인덱스르르 반환
const findIndex = arr3.findIndex(ele => ele>3);
console.log(findIndex);
/**`=>` 는 **화살표 함수(Arrow Function)** 를 만드는 기호야.
아주 쉽게 설명해줄게!

---

# 🎯 **`=>` 의 역할은?**

👉 **“여기서부터 함수 본문이 시작된다”** 라는 뜻이야.
즉, **함수를 간단하게 표현하는 문법**이 바로 화살표 함수야.

---

# 📌 네 코드에서 어떤 역할을 하는지

```js
ele => ele > 3
```

이 부분은 사실 아래와 **완전히 똑같은 함수**야:

```js
function(ele) {
    return ele > 3;
}
```

---

# 🧠 아주 쉽게 말하면…

* `ele` → 매개변수(지금 검사할 요소)
* `=>` → “이 값으로 판단해 주세요”
* `ele > 3` → true 또는 false를 반환하는 조건식

---

# 📌 find가 화살표 함수를 어떻게 쓰는지

```js
const find = arr3.find(ele => ele > 3);
```

의 의미는:

> “arr3에서 ele > 3 조건을 만족하는 **첫 번째 요소**를 찾아줘.”

---

# 🎉 한 줄 정리

👉 **`=>` 는 함수를 만들 때 쓰는 문법이며, “화살표 함수”라고 부른다.**

---

 
 */
//sort: 배열의 요소 순서를 정렬

const numbers = [4,2,9,1,5];
//리턴 결과가 양수가 나오면 앞의 값이 큼 > 서로 자리를 바꿈 ... 연속적으로 계속해서 정렬
numbers.sort((a,b)=>a-b);//오름차순 (ascending) 정렬
console.log(numbers); //[ 1, 2, 4, 5, 9 ]

const numbers1 = [4,2,9,1,5];
//리턴 결과가 음수가 나오면 앞의 값이 작음 > 서로 자리를 바꿈 ... 연속적으로 계속해서 정렬
numbers.sort((a,b)=>b-a);//내림차순 (ascending) 정렬
console.log(numbers1); //[ 9, 5, 4, 2, 1 ]

//오름차순 정렬 후 reverse하면 내림차순, 내림차순 정렬 후 reverse하면 오름차순
//리턴 결과가 0이 나오면 서로 자리를 바꾸지 않음. 
const words = ['banana','apple','cherry','peach'];
//{ } → 객체(Object)
//[ ] → 배열(Array)
words.sort(); //오름 차순 정렬
console.log(words);//[ 'apple', 'banana', 'cherry', 'peach' ]
words.sort((a,b)=>a<b?1:-1); //내림차순 정렬, 문자의 아스키코드값 비교
 
// String.fromCharCode(65); // "A"
// "A".charCodeAt(0); // 65

//실습

const hanwords = ['홍길동','강감찬','이순신','최영','권율'];

//한글 오름차순 정렬
hanwords.sort();
console.log(hanwords);
//한글 내림차순 정렬

 
// 내림차순
 hanwords.sort((a, b) => b.localeCompare(a));
console.log(hanwords);